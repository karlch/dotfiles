# Starting by updating
sudo apt-get update && sudo apt-get upgrade

# Important installations
sudo apt-get install unity-tweak-tool compiz-config-settings-manager
sudo apt-get install ubuntu-tweak tmux zsh ctags rubber

# zsh as shell
chsh -s "zsh"
